## Full-window surface alignment

- Removes the remaining vertical inset around the two-pane Studio surface so the sidebar and Canvas reach the native window's top and bottom content boundaries cleanly.
- Keeps the finite viewport layout that prevents macOS 26 from vertically centering or clipping the sidebar header, connection footer, and Canvas content.
- Preserves the existing theme application, bundled theme library, DockDoor launcher, and Sparkle update behavior.
