# Codex Studio 0.1.22

Fixes welcome text that remained shifted left after the ChatGPT 26.915.31029
update. The theme now preserves the new welcome container's native width and
centering, aligning the heading with the main workspace and composer.

Also preserves project-name wrapping in narrow windows. Includes DreamSkin
runtime 1.9.8; existing local wallpaper libraries and favorites are retained.
